# kube-state-metrics is the only chart in this scenario that ships cluster
# state metrics (kube_pod_status_ready, kube_node_info, ...). The name
# override keeps the Service discoverable under a stable name that the
# Alloy discovery.relabel rule matches on.
fullnameOverride: kube-state-metrics
